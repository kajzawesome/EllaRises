<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      margin-top: 50px;
    }
    .status-code {
      font-size: 72px;
      color: #d9534f;
      margin-bottom: 20px;
    }
    .message {
      font-size: 24px;
      margin-bottom: 20px;
    }
    .countdown {
      font-size: 20px;
      color: #555;
    }
  </style>
</head>
<body>
  <div class="status-code">418</div>
  <div class="message"><%= message %></div>
  <div class="countdown">Redirecting to homepage in <span id="seconds">10</span> seconds...</div>

  <script>
    let seconds = 10;
    const counter = document.getElementById('seconds');

    const interval = setInterval(() => {
      seconds--;
      counter.textContent = seconds;
      if (seconds <= 0) {
        clearInterval(interval);
        window.location.href = '/';
      }
    }, 1000);
  </script>
</body>
</html>
